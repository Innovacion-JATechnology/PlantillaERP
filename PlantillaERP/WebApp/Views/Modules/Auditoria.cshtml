@{
    ViewData["Title"] = "Auditoría";
    ViewData["Breadcrumbs"] = new List<(string, string)>
    {
        ("Inicio", Url.Action("Index", "Home")),
        ("Administración", Url.Action("Administracion", "Modules")),
        ("Auditoría", null)
    };
}

<link rel="stylesheet" href="~/css/professional-tables.css" />

<div class="professional-page-container">
    <!-- Header Profesional -->
    <div class="professional-header fade-in-up">
        <h1>
            <i class="fas fa-file-shield"></i>
            Auditoría del Sistema
        </h1>
        <p>Monitorea todas las acciones realizadas en el sistema para seguridad y trazabilidad</p>
    </div>

    <!-- Toolbar con filtros -->
    <div class="professional-toolbar">
        <div></div>
        <button class="professional-btn professional-btn-outline">
            <i class="fas fa-filter"></i>
            Filtrar Registros
        </button>
    </div>

    <!-- Tabla de Auditoría -->
    <div class="professional-table-card">
        <div class="card-header">
            <h6>
                <i class="fas fa-eye"></i>
                Registro de Auditoría
            </h6>
        </div>
        <div class="card-body">
            <table class="professional-table">
                <thead>
                    <tr>
                        <th>Fecha y Hora</th>
                        <th>Usuario</th>
                        <th>Módulo</th>
                        <th>Acción</th>
                        <th>Detalles</th>
                        <th>IP</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td colspan="7">
                            <div class="empty-state">
                                <i class="fas fa-clipboard-list"></i>
                                <h5>No hay registros disponibles</h5>
                                <p>Los registros de auditoría del sistema aparecerán aquí</p>
                            </div>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>
