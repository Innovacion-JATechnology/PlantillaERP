@{
    ViewData["Title"] = "Ajustes de Inventario";
    ViewData["Breadcrumbs"] = new List<(string, string)>
    {
        ("Dashboard", Url.Action("Index", "Home")),
        ("Inventario", Url.Action("Inventario", "Modules")),
        ("Ajustes", Url.Action("AjustesInventario", "Modules"))
    };
}

<div class="container-fluid">
        </ol>
    </nav>

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3">@ViewData["Title"]</h1>
        <button class="btn btn-primary">
            <i class="fas fa-plus me-2"></i>Nuevo Ajuste
        </button>
    </div>

    <div class="card shadow">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">
                <i class="fas fa-sliders-h me-2"></i>Ajustes Realizados
            </h6>
        </div>
        <div class="card-body">
            <div class="alert alert-info">Sin ajustes disponibles</div>
        </div>
    </div>
</div>
