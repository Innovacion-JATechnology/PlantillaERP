@{
    ViewData["Title"] = "Diagrama Gantt";
    ViewData["Breadcrumbs"] = new List<(string, string)>
    {
        ("Inicio", Url.Action("Index", "Home")),
        ("Proyectos", Url.Action("Proyectos", "Modules")),
        ("Diagrama Gantt", Url.Action("DiagramaGantt", "Modules"))
    };
}

<div class="container-fluid">
        </ol>
    </nav>

    <h1 class="h3 mb-4">@ViewData["Title"]</h1>

    <div class="card shadow">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">
                <i class="fas fa-chart-gantt me-2"></i>Cronograma de Proyectos
            </h6>
        </div>
        <div class="card-body">
            <div class="alert alert-info">Diagrama de Gantt - Sin datos disponibles</div>
        </div>
    </div>
</div>
