@{
    ViewData["Title"] = "Equipos";
    ViewData["Breadcrumbs"] = new List<(string, string)>
    {
        ("Dashboard", Url.Action("Index", "Home")),
        ("Mantenimiento", Url.Action("Mantenimiento", "Modules")),
        ("Equipos", Url.Action("Equipos", "Modules"))
    };
}

<div class="container-fluid">
        </ol>
    </nav>

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3">@ViewData["Title"]</h1>
        <button class="btn btn-primary">
            <i class="fas fa-plus me-2"></i>Registrar Equipo
        </button>
    </div>

    <div class="card shadow">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">
                <i class="fas fa-tools me-2"></i>Equipos Disponibles
            </h6>
        </div>
        <div class="card-body">
            <div class="alert alert-info">Sin equipos registrados</div>
        </div>
    </div>
</div>
